rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 로그인한 본인의 저장 기사만 읽고 쓸 수 있음
    match /users/{userId}/saved/{articleId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
